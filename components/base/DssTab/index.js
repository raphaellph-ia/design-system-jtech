/**
 * ==========================================================================
 * DssTab — Barrel Export
 * ==========================================================================
 *
 * Exporta: componente principal, types e composables.
 *
 * @version 1.0.0
 */

// Componente principal (entry point wrapper)
export { default as DssTab } from './DssTab.vue'

// Types
export * from './types/tab.types'

// Composables
export { useTabClasses } from './composables/useTabClasses'
